mode: Mainline
branches: 
  master:
    regex: ^[master|main]$
    tag: 'beta'
    is-release-branch: true
    is-mainline: true
  pull-request:
    regex: ^(pull|pull\-requests|pr)[/-]
    tag: pr
    is-mainline: false
    is-release-branch: false